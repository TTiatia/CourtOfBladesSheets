{
    "name": "@roll20sheets/courtofblades",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "dev": "astro dev",
        "start": "astro dev",
        "build": "astro build",
        "preview": "astro preview",
        "sass-build": "sass --no-source-map src/styles/sheet.scss src/styles/sheet.css",
        "sass-dev": "sass --watch --no-source-map src/styles/sheet.scss src/styles/sheet.css"
    },
    "devDependencies": {
        "astro": "^1.0.0-beta.44",
        "sass": "^1.52.3"
    }
}